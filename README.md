# sample_images/

Place your local dataset here when running outside Colab. Expected layout:

```
sample_images/
└── CT scan Images (limited)/
    ├── Healthy/
    └── Tumor/
```

This folder is gitignored (except this README) so dataset files are not committed.
